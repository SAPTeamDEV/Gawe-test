name: .NET CI

on: [pull_request, push]

jobs:
  build:
    strategy:
      matrix:
        os: [windows-latest, ubuntu-latest, macos-latest]
        configuration: [Debug, Release]
    uses: SAPTeamDEV/Gawe/.github/workflows/blank.yml@main
    with:
      os: ${{ matrix.os }}
      configuration: ${{ matrix.configuration }}
